services:
  traefik:
    image: traefik:v3.3
    container_name: test_factory_scale_traefik
    command:
      - --providers.docker=false
      - --providers.file.filename=/etc/traefik/dynamic.yml
      - --entrypoints.web.address=:80
      - --ping=true
      - --ping.entrypoint=web
      - --accesslog=true
      - --api.dashboard=true
      - --api.basepath=/traefik
      - --serversTransport.forwardingTimeouts.dialTimeout=5s
      - --serversTransport.forwardingTimeouts.responseHeaderTimeout=30s
      - --serversTransport.forwardingTimeouts.idleConnTimeout=90s
    ports:
      - "${SCALE_HTTP_HOST_PORT:-8101}:80"
    volumes:
      - ./deploy/traefik/dynamic.scaling.yml:/etc/traefik/dynamic.yml:ro
    depends_on:
      web:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    networks:
      - edge
      - app

  web:
    build:
      context: .
      dockerfile: cintafactory/dockerfile
    environment:
      DJANGO_SETTINGS_MODULE: cintafactory.settings
      DEBUG: 1
      DJANGO_ALLOWED_HOSTS: ${DJANGO_ALLOWED_HOSTS:-*}
      DATABASE_HOST: pgbouncer
      DATABASE_PORT: 6432
      DATABASE_NAME: test_factory_db
      DATABASE_USER: test_factory_user
      DATABASE_PASSWORD: test_factory_pass
      RUN_MIGRATIONS: 1
      ASYNC_JOBS_RUNNER_MODE: external
      EMAIL_BACKEND: ${EMAIL_BACKEND:-django.core.mail.backends.smtp.EmailBackend}
      EMAIL_HOST: ${EMAIL_HOST:-smtp.office365.com}
      EMAIL_PORT: ${EMAIL_PORT:-587}
      EMAIL_HOST_USER: ${EMAIL_HOST_USER:-}
      EMAIL_HOST_PASSWORD: ${EMAIL_HOST_PASSWORD:-}
      EMAIL_USE_TLS: ${EMAIL_USE_TLS:-1}
      EMAIL_USE_SSL: ${EMAIL_USE_SSL:-0}
      EMAIL_TIMEOUT: ${EMAIL_TIMEOUT:-10}
      DEFAULT_FROM_EMAIL: ${DEFAULT_FROM_EMAIL:-webmaster@localhost}
      SERVER_EMAIL: ${SERVER_EMAIL:-webmaster@localhost}
      SEAWEEDFS_FILER_URL: ${SEAWEEDFS_FILER_URL:-http://seaweedfs:8888}
      SEAWEEDFS_PUBLIC_URL: ${SEAWEEDFS_PUBLIC_URL:-http://seaweedfs:8888}
      SEAWEEDFS_BASE_DIR: ${SEAWEEDFS_BASE_DIR:-media}
      SEAWEEDFS_TIMEOUT: ${SEAWEEDFS_TIMEOUT:-30}
      LIKEC4_EXPORT_URL: ${LIKEC4_EXPORT_URL:-http://likec4-exporter:9000/export}
      LIKEC4_EXPORT_DELETE_OLD: ${LIKEC4_EXPORT_DELETE_OLD:-1}
      LIKEC4_EXPORT_ENABLED: ${LIKEC4_EXPORT_ENABLED:-1}
      CLAMAV_HOST: ${CLAMAV_HOST:-clamav}
      CLAMAV_PORT: ${CLAMAV_PORT:-3310}
      CLAMAV_TIMEOUT: ${CLAMAV_TIMEOUT:-30}
      CLAMAV_SCAN_DIR: ${CLAMAV_SCAN_DIR:-/clamav_scan}
      DRAWIO_BASE_URL: ${DRAWIO_BASE_URL:-http://drawio:8080}
      DRAWIO_PUBLIC_URL: ${DRAWIO_PUBLIC_URL:-http://localhost:8101/diagrams/drawio/proxy/}
      DRAWIO_LIBS: ${DRAWIO_LIBS:-general}
      DRAWIO_CLIBS: ${DRAWIO_CLIBS:-}
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:-}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET:-}
      LIKEC4_METADATA_TOKEN: ${LIKEC4_METADATA_TOKEN:-}
    volumes:
      - ./cintafactory:/app
      - clamav_scan:/clamav_scan
    expose:
      - "8000"
    command: python manage.py runserver 0.0.0.0:8000
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "python", "manage.py", "check_runtime_dependencies", "--profile", "web"]
      interval: 30s
      timeout: 10s
      retries: 5
    depends_on:
      pgbouncer:
        condition: service_started
      seaweedfs:
        condition: service_started
      clamav:
        condition: service_started
      likec4-exporter:
        condition: service_started
      drawio-export:
        condition: service_started
    networks:
      - app

  worker:
    build:
      context: .
      dockerfile: cintafactory/dockerfile
    environment:
      DJANGO_SETTINGS_MODULE: cintafactory.settings
      DEBUG: 1
      DATABASE_HOST: pgbouncer
      DATABASE_PORT: 6432
      DATABASE_NAME: test_factory_db
      DATABASE_USER: test_factory_user
      DATABASE_PASSWORD: test_factory_pass
      RUN_MIGRATIONS: 0
      ASYNC_JOBS_RUNNER_MODE: external
      SEAWEEDFS_FILER_URL: ${SEAWEEDFS_FILER_URL:-http://seaweedfs:8888}
      SEAWEEDFS_PUBLIC_URL: ${SEAWEEDFS_PUBLIC_URL:-http://seaweedfs:8888}
      SEAWEEDFS_BASE_DIR: ${SEAWEEDFS_BASE_DIR:-media}
      SEAWEEDFS_TIMEOUT: ${SEAWEEDFS_TIMEOUT:-30}
      LIKEC4_EXPORT_URL: ${LIKEC4_EXPORT_URL:-http://likec4-exporter:9000/export}
      LIKEC4_EXPORT_DELETE_OLD: ${LIKEC4_EXPORT_DELETE_OLD:-1}
      LIKEC4_EXPORT_ENABLED: ${LIKEC4_EXPORT_ENABLED:-1}
      CLAMAV_HOST: ${CLAMAV_HOST:-clamav}
      CLAMAV_PORT: ${CLAMAV_PORT:-3310}
      CLAMAV_TIMEOUT: ${CLAMAV_TIMEOUT:-30}
      CLAMAV_SCAN_DIR: ${CLAMAV_SCAN_DIR:-/clamav_scan}
      DRAWIO_BASE_URL: ${DRAWIO_BASE_URL:-http://drawio:8080}
    volumes:
      - ./cintafactory:/app
      - clamav_scan:/clamav_scan
    command: python manage.py run_async_job_worker --poll-interval 0.5
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    depends_on:
      pgbouncer:
        condition: service_started
      seaweedfs:
        condition: service_started
      clamav:
        condition: service_started
      likec4-exporter:
        condition: service_started
      drawio-export:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "ps -ef | grep -q '[r]un_async_job_worker' && python manage.py check_runtime_dependencies --profile worker"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - app

  db:
    image: postgres:16
    container_name: test_factory_scale_db
    environment:
      POSTGRES_DB: test_factory_db
      POSTGRES_USER: test_factory_user
      POSTGRES_PASSWORD: test_factory_pass
    volumes:
      - pgdata_dev:/var/lib/postgresql/data
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_factory_user -d test_factory_db -h 127.0.0.1 -p 5432"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - data

  pgbouncer:
    image: edoburu/pgbouncer:latest
    container_name: test_factory_scale_pgbouncer
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: test_factory_user
      DB_PASSWORD: test_factory_pass
      AUTH_TYPE: scram-sha-256
      LISTEN_PORT: 6432
      POOL_MODE: transaction
      DEFAULT_POOL_SIZE: ${PGBOUNCER_DEFAULT_POOL_SIZE:-40}
      MAX_CLIENT_CONN: ${PGBOUNCER_MAX_CLIENT_CONN:-300}
    expose:
      - "6432"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app
      - data

  drawio:
    image: jgraph/drawio
    container_name: test_factory_scale_drawio
    environment:
      EXPORT_URL: http://drawio-export:8000/export
    expose:
      - "8080"
    depends_on:
      drawio-export:
        condition: service_started
    networks:
      - app

  drawio-export:
    image: jgraph/export-server
    expose:
      - "8000"
    healthcheck:
      test: ["CMD-SHELL", "wget -S --spider http://127.0.0.1:8000/export 2>&1 | grep -q 'HTTP/1.1 400'"]
      interval: 15s
      timeout: 5s
      retries: 10
    networks:
      - app

  likec4:
    image: likec4-cli
    container_name: test_factory_scale_likec4
    build:
      context: ./likec4
      dockerfile: Dockerfile
      args:
        GRAPHVIZ_VERSION: ${LIKEC4_GRAPHVIZ_VERSION:-}
        PLAYWRIGHT_VER: ${LIKEC4_PLAYWRIGHT_VER:-}
        LIKEC4_VER: ${LIKEC4_VER:-}
    entrypoint: ["/bin/sh", "-c"]
    command: ["node server.js & mkdir -p ${LIKEC4_PREVIEW_DIR:-/tmp/likec4-previews} && touch ${LIKEC4_PREVIEW_DIR:-/tmp/likec4-previews}/bootstrap.c4 && /usr/local/bin/likec4 dev ${LIKEC4_PREVIEW_DIR:-/tmp/likec4-previews} --host 0.0.0.0"]
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    read_only: true
    tmpfs:
      - /tmp
    environment:
      C4_FILE: ${LIKEC4_C4_FILE:-likec4/default.c4}
      LIKEC4_EDITOR_PORT: ${LIKEC4_EDITOR_PORT:-4173}
      LIKEC4_PREVIEW_DIR: ${LIKEC4_PREVIEW_DIR:-/tmp/likec4-previews}
      SEAWEEDFS_FILER_URL: ${SEAWEEDFS_FILER_URL:-http://seaweedfs:8888}
      SEAWEEDFS_BASE_DIR: ${SEAWEEDFS_BASE_DIR:-media}
      LIKEC4_METADATA_URL: http://web:8000/diagrams/likec4/metadata/
      LIKEC4_METADATA_TOKEN: ${LIKEC4_METADATA_TOKEN:-}
    expose:
      - "5173"
      - "4173"
      - "24678"
    volumes:
      - ./likec4:/data
    networks:
      - app

  likec4-exporter:
    image: likec4-cli
    build:
      context: ./likec4
      dockerfile: Dockerfile
      args:
        GRAPHVIZ_VERSION: ${LIKEC4_GRAPHVIZ_VERSION:-}
        PLAYWRIGHT_VER: ${LIKEC4_PLAYWRIGHT_VER:-}
        LIKEC4_VER: ${LIKEC4_VER:-}
    command: ["node", "/opt/likec4-worker/exporter.js"]
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    read_only: true
    tmpfs:
      - /tmp
    environment:
      LIKEC4_EXPORT_PORT: ${LIKEC4_EXPORT_PORT:-9000}
      LIKEC4_EXPORT_FORMAT: ${LIKEC4_EXPORT_FORMAT:-png}
      LIKEC4_EXPORT_VIEW: ${LIKEC4_EXPORT_VIEW:-}
      LIKEC4_EXPORT_MAX_RETRIES: ${LIKEC4_EXPORT_MAX_RETRIES:-3}
      LIKEC4_EXPORT_DELETE_OLD: ${LIKEC4_EXPORT_DELETE_OLD:-1}
      LIKEC4_EXPORT_LOCAL_DIR: ${LIKEC4_EXPORT_LOCAL_DIR:-/var/likec4-exports}
      SEAWEEDFS_FILER_URL: ${SEAWEEDFS_FILER_URL:-http://seaweedfs:8888}
      SEAWEEDFS_BASE_DIR: ${SEAWEEDFS_BASE_DIR:-media}
      LIKEC4_METADATA_URL: http://web:8000/diagrams/likec4/metadata/
      LIKEC4_METADATA_TOKEN: ${LIKEC4_METADATA_TOKEN:-}
    volumes:
      - likec4_exports_dev:/var/likec4-exports
    depends_on:
      seaweedfs:
        condition: service_started
    expose:
      - "9000"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:9000/ >/dev/null || curl -fsS http://127.0.0.1:9000/health >/dev/null || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
    networks:
      - app

  clamav:
    image: clamav/clamav:latest
    container_name: test_factory_scale_clamav
    volumes:
      - clamav_scan:/clamav_scan
    expose:
      - "3310"
    networks:
      - app

  seaweedfs:
    image: chrislusf/seaweedfs:latest
    container_name: test_factory_scale_seaweedfs
    command: "server -dir=/data -master -volume -filer"
    expose:
      - "9333"
      - "8888"
      - "8080"
    volumes:
      - seaweedfs_data:/data
    networks:
      - app

volumes:
  pgdata_dev:
  seaweedfs_data:
  likec4_exports_dev:
  clamav_scan:

networks:
  edge:
  app:
  data:
